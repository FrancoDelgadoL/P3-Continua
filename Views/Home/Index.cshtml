@model List<P3_Continua.Models.Post>

<h1>Lista de Títulos</h1>

<ul>
@foreach (var post in Model)
{
    <li>
        <a href="@Url.Action("Details", "Home", new { id = post.Id })">
            @post.Title
        </a>
    </li>
}
</ul>
